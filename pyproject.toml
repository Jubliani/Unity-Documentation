[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Unity Documentation"
authors = [{name = "Jubliani", email = "davidspitz900@gmail.com"}]
dynamic = ["version", "description"]
